---
phase: 04-autocomplete-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SearchBar.tsx
autonomous: true
requirements:
  - AUTO-01
  - AUTO-02
  - AUTO-03
  - AUTO-04
tags:
  - frontend
  - react
  - aria
  - autocomplete

must_haves:
  truths:
    - "Typing 2+ characters in the SearchBar input fires a fetch to /api/v1/cities/search after exactly 200ms of idle time"
    - "Rapidly typing additional characters cancels the previous in-flight fetch (AbortController) — only the final query's response updates state"
    - "Up to 8 suggestions render in 'City, Region, Country' format (region omitted when null), ranked by population DESC (server-side)"
    - "When the API returns an empty array, the dropdown shows the exact text \"No cities found for '{query}'\" (with the user's typed query interpolated)"
    - "ArrowDown moves the highlighted suggestion down (clamped at last index); ArrowUp moves up (clamped at first index); both prevent default to stop page scroll"
    - "Enter adds the highlighted city via onAddCity when highlightedIndex >= 0; Enter is a no-op when highlightedIndex === -1"
    - "Escape closes the dropdown and resets highlightedIndex to -1"
    - "Tab closes the dropdown and resets highlightedIndex to -1, but does NOT preventDefault (focus moves to next element naturally)"
    - "The typed prefix appears bold (rendered via <strong>) in the city-name portion of each suggestion row; region and country remain plain text"
    - "The input has role=\"combobox\", aria-expanded bound to showDropdown, aria-controls bound to the listbox id (from useId), aria-autocomplete=\"list\", and aria-activedescendant pointing to the highlighted <li> id (or undefined when index is -1)"
    - "The <ul> has role=\"listbox\" with the same id referenced by aria-controls; each suggestion <li> has role=\"option\", id matching the getOptionId(i) formula, aria-selected={i === highlightedIndex}, and tabIndex={-1}"
    - "Suggestion <li> selection is wired via onMouseDown with e.preventDefault() (NOT <button> + onClick) so selection fires before the input's onBlur closes the dropdown"
    - "TypeScript compile is clean: npx tsc --noEmit exits with code 0"
  artifacts:
    - path: "src/components/SearchBar.tsx"
      provides: "Full ARIA combobox autocomplete with debounce + AbortController + keyboard nav + prefix highlight"
      min_lines: 150
      contains:
        - "import { useState, useRef, useEffect, useId } from 'react'"
        - "const abortRef = useRef<AbortController | null>(null)"
        - "const [highlightedIndex, setHighlightedIndex] = useState(-1)"
        - "const listboxId = useId()"
        - "function HighlightMatch"
        - "function formatSuggestion"
        - "role=\"combobox\""
        - "role=\"listbox\""
        - "role=\"option\""
        - "aria-activedescendant"
        - "onMouseDown"
        - "}, 200)"
        - "No cities found for"
  key_links:
    - from: "src/components/SearchBar.tsx handleChange()"
      to: "/api/v1/cities/search"
      via: "fetch with AbortController signal, inside setTimeout(200ms) callback"
      pattern: "fetch\\(`/api/v1/cities/search\\?q=\\$\\{encodeURIComponent"
    - from: "src/components/SearchBar.tsx <input>"
      to: "src/components/SearchBar.tsx <ul role=\"listbox\">"
      via: "aria-controls + listboxId from useId()"
      pattern: "aria-controls=\\{listboxId\\}"
    - from: "src/components/SearchBar.tsx <input>"
      to: "src/components/SearchBar.tsx <li role=\"option\"> (highlighted)"
      via: "aria-activedescendant + getOptionId(highlightedIndex)"
      pattern: "aria-activedescendant"
    - from: "src/components/SearchBar.tsx handleSelect"
      to: "props.onAddCity (wired in page.tsx via useCityStorage.addCity)"
      via: "function prop invocation"
      pattern: "onAddCity\\(result\\)"
---

<objective>
Rewrite `src/components/SearchBar.tsx` to upgrade the current 116-line minimal stub into a full WAI-ARIA combobox with debounced+aborted fetch, keyboard navigation, prefix highlighting, and the exact "No cities found for '{input}'" empty state. This closes all four AUTO requirements in a single targeted file modification.

Purpose: Phase 04 goal — users can find and add any of 7,300 cities instantly via a type-ahead dropdown with full keyboard support.

Output: A single modified file, TypeScript-clean, that satisfies AUTO-01 through AUTO-04. No new files, no new dependencies, no other files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-autocomplete-ui/04-RESEARCH.md
@.planning/phases/04-autocomplete-ui/04-PATTERNS.md
@.planning/phases/04-autocomplete-ui/04-UI-SPEC.md
@src/components/SearchBar.tsx
@src/types/weather.ts
@src/app/api/v1/cities/search/route.ts
@src/hooks/useWeather.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/types/weather.ts (lines 63–71):
```typescript
export interface CityEntry {
  id: number;
  name: string;
  country: string;
  region: string | null;    // null-safe — use ternary, not && short-circuit
  lat: number;
  lon: number;
  timezone: string;
}
```

From src/components/SearchBar.tsx (current prop contract — DO NOT change):
```typescript
interface SearchBarProps {
  cities: CityEntry[]
  onAddCity: (city: CityEntry) => void
}
export function SearchBar({ cities, onAddCity }: SearchBarProps)
```

From src/app/api/v1/cities/search/route.ts (server contract — already complete from Phase 03):
- GET /api/v1/cities/search?q={query}
- Returns: CityEntry[] (max 8 rows, ORDER BY population DESC)
- Client must encodeURIComponent the query value before interpolation

React 19 hooks used (all available in react@19.2.4):
- useState, useRef, useEffect (already imported)
- useId (NEW import for this plan — SSR-safe stable id generation)
</interfaces>

<critical_constraints>
This is NOT the Next.js you know (per project AGENTS.md). React 19 + Next 16 in use. All hooks come from React 19 — `useId()` is available and required for ARIA id generation. No new npm dependencies are permitted in this plan (project lean-deps posture, verified by absence of UI libs across all phases).

No test framework exists in this repo — automated verification is `npx tsc --noEmit` only. Behavioral verification of keyboard / dropdown UX is manual-browser per UAT, NOT in this plan.
</critical_constraints>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite SearchBar.tsx with ARIA combobox, AbortController, keyboard nav, and prefix highlight</name>
  <files>src/components/SearchBar.tsx</files>
  <read_first>
    - src/components/SearchBar.tsx (current 116-line implementation — see what is preserved vs replaced)
    - .planning/phases/04-autocomplete-ui/04-RESEARCH.md (Patterns 1–5, Anti-Patterns, Pitfalls 1–7, Open Questions)
    - .planning/phases/04-autocomplete-ui/04-PATTERNS.md (line-by-line rewrite map: Patterns 1–7, "Preserved Code" table, "Summary of Changes" table)
    - .planning/phases/04-autocomplete-ui/04-UI-SPEC.md (full attribute contracts for input / ul / li, copywriting contract, state inventory)
    - src/types/weather.ts (CityEntry shape — confirm region is string | null)
    - src/hooks/useWeather.ts (codebase AbortController/cancelled-flag analog pattern)
    - src/app/api/v1/cities/search/route.ts (server response shape — already returns CityEntry[] with LIMIT 8)
  </read_first>
  <action>
    Modify `src/components/SearchBar.tsx` per the combined contract in 04-PATTERNS.md "Summary of Changes" table and 04-UI-SPEC.md attribute tables. Apply every change listed below; do not omit any:

    1. React import: change `import { useState, useRef, useEffect } from 'react'` to add `useId` (now imports useState, useRef, useEffect, useId).

    2. Preserve unchanged: `'use client'` directive, `SearchBarProps` interface, function signature, all five existing state declarations (query, results, showDropdown, duplicateError), containerRef, debounceRef, dupTimerRef, the click-outside useEffect on document mousedown, the existing handleSelect body (duplicate check / 10-city cap / clear-on-select), the isFull derivation, the outer <div ref={containerRef} className="relative w-full max-w-md">, the input's existing Tailwind className string, the duplicate-error <p className="mt-1 text-sm text-red-300">.

    3. Add a new useState: `highlightedIndex` of type number, initial value -1. This represents the keyboard-navigated suggestion index; -1 means nothing highlighted.

    4. Add a new useRef: `abortRef` of type `AbortController | null`, initial value null. Declare it next to debounceRef.

    5. Add two derived id helpers after the ref declarations: `const listboxId = useId()` and `const getOptionId = (i: number) => ${listboxId}-opt-${i}` (template literal). These ids are used in three places: id on the <ul>, id on each suggestion <li>, and aria-activedescendant on the <input>.

    6. Declare two new helpers OUTSIDE the SearchBar function (at module scope, above `export function SearchBar`):
       - `HighlightMatch({ text, query })` — if query is empty or text does not start with query case-insensitively, returns the plain text fragment; otherwise splits text into prefix (text.slice(0, query.length)) and rest (text.slice(query.length)) and returns prefix wrapped in <strong> followed by rest. Pure JSX — never dangerouslySetInnerHTML.
       - `formatSuggestion(city: CityEntry, query: string): React.ReactNode` — returns a fragment containing <HighlightMatch text={city.name} query={query} /> followed by the suffix string. Suffix is `, ${city.region}, ${city.country}` when city.region is truthy, else `, ${city.country}`. The region/country portion is plain text — only the city name gets HighlightMatch.

    7. Update handleChange: at the top, after `setDuplicateError(false)`, add `setHighlightedIndex(-1)` to reset stale index. After `clearTimeout(debounceRef.current)`, add `if (abortRef.current) abortRef.current.abort()` to cancel any in-flight fetch. Inside the setTimeout callback, the FIRST line must be `const controller = new AbortController()` and the SECOND line must be `abortRef.current = controller` (controller created inside the callback, not outside — see Pitfall 2 in RESEARCH.md). Pass `{ signal: controller.signal }` as the second argument to fetch. After `setResults(data)`, set `setShowDropdown(true)` unconditionally on a successful response (so the "No cities found for…" empty state is visible). Wrap the fetch in try/catch; in catch, only suppress when `err instanceof Error && err.name === 'AbortError'`. Change the setTimeout delay from 300 to 200 (AUTO-01 — current literal is 300; the correct value is 200).

    8. Replace handleKeyDown entirely. Guard `if (!showDropdown) return` at the top. Switch on e.key with cases:
       - 'ArrowDown': e.preventDefault(); setHighlightedIndex((i) => Math.min(i + 1, results.length - 1))
       - 'ArrowUp': e.preventDefault(); setHighlightedIndex((i) => Math.max(i - 1, 0))
       - 'Enter': e.preventDefault(); if (highlightedIndex >= 0 && results[highlightedIndex]) handleSelect(results[highlightedIndex])
       - 'Escape': setShowDropdown(false); setHighlightedIndex(-1)
       - 'Tab': setShowDropdown(false); setHighlightedIndex(-1) — do NOT call e.preventDefault (focus must move naturally)

    9. handleSelect: after the duplicate-cap-clear logic (i.e., after `setShowDropdown(false)`), add `setHighlightedIndex(-1)`.

    10. Update the <input> JSX. Keep type, value, onChange, onKeyDown, placeholder, and the full existing className. Add these ARIA attributes:
        - role="combobox"
        - aria-expanded={showDropdown}
        - aria-controls={listboxId}
        - aria-autocomplete="list"
        - aria-activedescendant={highlightedIndex >= 0 ? getOptionId(highlightedIndex) : undefined}

    11. Update the <ul> JSX. Add role="listbox" and id={listboxId}. Keep the existing className.

    12. Replace the empty-state <li>. Use:
        - role="option"
        - aria-disabled="true"
        - className "px-4 py-2 text-sm text-gray-500"
        - text content: No cities found for '{query}' (the apostrophes are real single-quote characters wrapping {query}; use &apos; for the literal single quotes around the interpolation so JSX parses cleanly per 04-PATTERNS.md Pattern 7)

    13. Replace each suggestion <li> entirely — remove the inner <button> wrapper. The <li> itself carries:
        - key={r.id}
        - role="option"
        - id={getOptionId(i)} (where i is the map index)
        - aria-selected={i === highlightedIndex}
        - tabIndex={-1}
        - onMouseDown={(e) => { e.preventDefault(); if (!isFull) handleSelect(r) }} — onMouseDown NOT onClick (fires before onBlur), e.preventDefault() prevents input from losing focus
        - className combining base "px-4 py-2 text-sm text-gray-800 cursor-pointer", plus `bg-blue-100` when i === highlightedIndex else `hover:bg-blue-50`, plus `opacity-50 cursor-not-allowed` when isFull
        - children: {formatSuggestion(r, query)}

    14. Do not introduce new state, new refs, new effects, new imports, new components, or new files beyond what is enumerated above. Do not modify any file other than src/components/SearchBar.tsx.

    Estimated final size: ~180–185 lines. Pitfall references: see 04-RESEARCH.md Pitfalls 1 (debounce 200ms not 300ms), 2 (controller inside callback), 3 (reset highlightedIndex on change), 4 (matching aria-activedescendant id), 5 (no preventDefault on Tab), 6 (no <button> in <li role="option">), 7 (mousedown not click).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/SumpleWish/Desktop/ai-course/project && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compile is clean: `npx tsc --noEmit` exits 0
    - `grep -c "useId" src/components/SearchBar.tsx` returns at least 2 (one in import, one in declaration)
    - `grep -c "abortRef" src/components/SearchBar.tsx` returns at least 4 (ref declaration, abort call in handleChange, assignment inside setTimeout, type annotation)
    - `grep -c "highlightedIndex" src/components/SearchBar.tsx` returns at least 8 (declaration, setter use, reset in handleChange, four key cases, aria-activedescendant, aria-selected, reset in handleSelect)
    - `grep -n "}, 200)" src/components/SearchBar.tsx` finds exactly one match (the debounce setTimeout closer); no occurrence of `}, 300)` remains
    - `grep -c "AbortError" src/components/SearchBar.tsx` returns at least 1
    - `grep -c "role=\"combobox\"" src/components/SearchBar.tsx` returns exactly 1
    - `grep -c "role=\"listbox\"" src/components/SearchBar.tsx` returns exactly 1
    - `grep -c "role=\"option\"" src/components/SearchBar.tsx` returns at least 2 (no-results <li> and result <li>)
    - `grep -c "aria-expanded" src/components/SearchBar.tsx` returns exactly 1
    - `grep -c "aria-controls" src/components/SearchBar.tsx` returns exactly 1
    - `grep -c "aria-activedescendant" src/components/SearchBar.tsx` returns exactly 1
    - `grep -c "aria-autocomplete=\"list\"" src/components/SearchBar.tsx` returns exactly 1
    - `grep -c "aria-selected" src/components/SearchBar.tsx` returns exactly 1
    - `grep -c "aria-disabled" src/components/SearchBar.tsx` returns exactly 1
    - `grep -c "tabIndex={-1}" src/components/SearchBar.tsx` returns at least 1
    - `grep -c "onMouseDown" src/components/SearchBar.tsx` returns at least 1 (the <li> selection handler)
    - `grep -c "<button" src/components/SearchBar.tsx` returns 0 (no <button> elements remain in the file)
    - `grep -c "dangerouslySetInnerHTML" src/components/SearchBar.tsx` returns 0
    - `grep -F "No cities found for" src/components/SearchBar.tsx` finds at least one line containing this substring AND containing `{query}` on the same JSX expression
    - `grep -c "HighlightMatch" src/components/SearchBar.tsx` returns at least 3 (declaration, usage inside formatSuggestion, plus type/JSX self-close or close)
    - `grep -c "formatSuggestion" src/components/SearchBar.tsx` returns at least 2 (declaration, usage in <li> children)
    - `grep -c "<strong>" src/components/SearchBar.tsx` returns at least 1
    - `grep -c "preventDefault" src/components/SearchBar.tsx` shows preventDefault used in ArrowDown, ArrowUp, Enter, and onMouseDown cases — and grep -B1 -A6 "case 'Tab'" src/components/SearchBar.tsx shows NO `preventDefault` call inside the Tab case
    - The fetch URL pattern matches `\`/api/v1/cities/search\\?q=\\$\\{encodeURIComponent\\(val\\)\\}\`` (verified by `grep -F "/api/v1/cities/search?q=\${encodeURIComponent(val)}" src/components/SearchBar.tsx` returning 1 match)
    - File ends with `}` closing `export function SearchBar` and parses as valid TypeScript (covered by tsc --noEmit)
    - SearchBarProps interface still has exactly the two original props: `cities: CityEntry[]` and `onAddCity: (city: CityEntry) => void` (no new props added)
    - `wc -l src/components/SearchBar.tsx` reports at least 150 lines
  </acceptance_criteria>
  <done>
    src/components/SearchBar.tsx satisfies every grep assertion in acceptance_criteria, TypeScript compiles clean, no other files are modified, and the file structure matches the 04-UI-SPEC.md attribute contracts for input / ul / li and the 04-PATTERNS.md Summary-of-Changes table.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (run after Task 1 completes):

1. Static / automated:
   - `npx tsc --noEmit` exits with code 0
   - All grep assertions in Task 1 acceptance_criteria pass
   - `git diff --stat` shows ONLY src/components/SearchBar.tsx changed

2. Manual browser verification (deferred to UAT — no test runner exists in repo, confirmed by RESEARCH.md §Validation Architecture):
   - AUTO-01: Open DevTools Network. Type "ban" fast in the SearchBar. Confirm exactly one request to /api/v1/cities/search?q=ban fires after ~200ms. Type "bang" before the response — confirm the prior request is cancelled (status: canceled) and a new one fires.
   - AUTO-02: Type "bang". Dropdown shows up to 8 rows in "City, Region, Country" form, with region omitted for cities where region is null (e.g., "Singapore, Singapore" with no region). Type "zzzzz" — dropdown shows "No cities found for 'zzzzz'" with the literal query string interpolated inside single quotes.
   - AUTO-03: Type "ban". Press ↓ — first row highlights blue (bg-blue-100). Press ↓ again — second row highlights. Press ↑ — back to first. Press Enter — city is added to the dashboard. Reopen with another query, press Escape — dropdown closes. Reopen, press Tab — dropdown closes AND focus moves to the next focusable element on the page.
   - AUTO-04: Type "bang". Each suggestion row's city name shows the prefix "Bang" in bold (rendered via <strong>), with the rest of the name in regular weight. Region and country remain plain weight.
</verification>

<success_criteria>
Phase 04 succeeds when:
- TypeScript compile is clean (automated)
- All grep assertions in Task 1 acceptance_criteria pass (automated)
- SearchBar.tsx is the only modified file (automated: `git diff --name-only` returns exactly one path)
- The four AUTO requirements are observably satisfied in the running browser (manual UAT)
- The file's ARIA, keyboard, debounce, abort, highlight, and empty-state behaviors match the contracts in 04-UI-SPEC.md and 04-PATTERNS.md without deviation
</success_criteria>

<output>
After completion, create `.planning/phases/04-autocomplete-ui/04-01-SUMMARY.md` documenting:
- What was changed (line-level diff summary against the original 116-line file)
- Which requirements were closed (AUTO-01, AUTO-02, AUTO-03, AUTO-04)
- TypeScript compile result
- Any deviations from the plan (if none, state "None")
- Pending verification items (manual browser UAT against AUTO-01..04 success criteria)
</output>
