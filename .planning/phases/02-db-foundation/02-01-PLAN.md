---
phase: 02-db-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
autonomous: true
requirements:
  - CITY-01
  - CITY-02
must_haves:
  truths:
    - "better-sqlite3 is installed as a runtime dependency (in package.json dependencies)"
    - "npm run build-db script entry exists in package.json scripts"
    - "next.config.ts contains serverExternalPackages: ['better-sqlite3'] (the Next.js 15+ key)"
    - "next.config.ts contains outputFileTracingIncludes with key '/*' and value './data/cities.db'"
    - "npm install completes without errors after package.json changes"
  artifacts:
    - path: "package.json"
      provides: "better-sqlite3 runtime dep + @types/better-sqlite3 devDep + build-db script"
      contains: "better-sqlite3"
    - path: "next.config.ts"
      provides: "webpack bundling exclusion + output tracing inclusion for cities.db"
      contains: "serverExternalPackages"
  key_links:
    - from: "next.config.ts"
      to: "better-sqlite3 native binary"
      via: "serverExternalPackages prevents webpack from bundling the .node binary"
      pattern: "serverExternalPackages.*better-sqlite3"
    - from: "next.config.ts"
      to: "data/cities.db"
      via: "outputFileTracingIncludes ensures the DB file survives next build tracing"
      pattern: "outputFileTracingIncludes"
---

<objective>
Install better-sqlite3 as a runtime dependency and configure Next.js to handle its native binary correctly.

Purpose: All subsequent Phase 02 and Phase 03 work requires better-sqlite3 to be installed. The two next.config.ts keys are critical safety guards — without them, the app crashes silently at runtime on Railway even when the DB file exists.

Output:
- package.json with better-sqlite3 in dependencies, @types/better-sqlite3 in devDependencies, and build-db npm script
- next.config.ts with serverExternalPackages and outputFileTracingIncludes
- node_modules updated via npm install
</objective>

<execution_context>
@/home/aiadmin/Documents/frontire AI/Frontire-AI-Coding-Group3/.planning/phases/02-db-foundation/02-01-PLAN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/aiadmin/Documents/frontire AI/Frontire-AI-Coding-Group3/.planning/ROADMAP.md
@/home/aiadmin/Documents/frontire AI/Frontire-AI-Coding-Group3/.planning/REQUIREMENTS.md

<interfaces>
<!-- Current state of files being modified. Executor MUST read before touching. -->

Current next.config.ts (full file — 7 lines):
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
```

Current package.json scripts block:
```json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "lint": "eslint"
}
```

Current package.json dependencies block:
```json
"dependencies": {
  "next": "16.2.6",
  "react": "19.2.4",
  "react-dom": "19.2.4"
}
```

Current package.json devDependencies block:
```json
"devDependencies": {
  "@tailwindcss/postcss": "^4",
  "@types/node": "^20",
  "@types/react": "^19",
  "@types/react-dom": "^19",
  "eslint": "^9",
  "eslint-config-next": "16.2.6",
  "tailwindcss": "^4",
  "typescript": "^5"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add better-sqlite3 dependency and build-db npm script to package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json — read the full file before editing to see current scripts, dependencies, and devDependencies blocks
  </read_first>
  <action>
    Edit package.json to make three additions:

    1. In the "scripts" block, add "build-db" entry after "lint":
       "build-db": "node scripts/build-cities-db.mjs"

    2. In the "dependencies" block, add after "react-dom":
       "better-sqlite3": "^12.4.1"

    3. In the "devDependencies" block, add after "@tailwindcss/postcss" (maintain alphabetical order):
       "@types/better-sqlite3": "latest"

    After editing package.json, run: npm install

    CRITICAL: better-sqlite3 MUST be in "dependencies" (not "devDependencies"). Railway's NIXPACKS build runs npm install --production by default; if better-sqlite3 is in devDependencies, the native binary will not be compiled during the Railway build and the app will crash at runtime.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); console.log(JSON.stringify({dep:p.dependencies['better-sqlite3'], devDep:p.devDependencies['@types/better-sqlite3'], script:p.scripts['build-db']}))"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "better-sqlite3": "^12.4.1" inside the "dependencies" block (NOT devDependencies)
    - package.json contains "@types/better-sqlite3": "latest" inside "devDependencies"
    - package.json scripts block contains "build-db": "node scripts/build-cities-db.mjs"
    - npm install exits 0 with no errors
    - node_modules/better-sqlite3 directory exists after npm install
  </acceptance_criteria>
  <done>package.json has all three additions committed and npm install succeeds locally</done>
</task>

<task type="auto">
  <name>Task 2: Add serverExternalPackages and outputFileTracingIncludes to next.config.ts</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts — read the full file before editing (currently 7 lines; import and export lines must be preserved exactly)
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/serverExternalPackages.md — verify the key name in the installed Next.js version before editing
  </read_first>
  <action>
    Replace the body of the nextConfig object in next.config.ts. The import line and export line are unchanged. Replace the placeholder comment "/* config options here */" with both required keys.

    The two keys to add inside the NextConfig object:

    Key 1 — serverExternalPackages (per D-05):
    Array value: ['better-sqlite3']
    Add an inline comment above it: "// Prevents webpack from bundling better-sqlite3's native .node binary."
    Add a second comment line: "// Use serverExternalPackages (Next.js 15+), NOT serverComponentsExternalPackages."

    Key 2 — outputFileTracingIncludes (per D-06):
    Object value with key '/*' mapping to array ['./data/cities.db']
    Add an inline comment above it: "// Ensures data/cities.db is included in output file tracing for Railway."

    CRITICAL: The key name is "serverExternalPackages" — NOT "serverComponentsExternalPackages". The old name was renamed at Next.js 15.0.0; using it silently does nothing in Next.js 15+.

    Maintain the existing file's double-quote style for string imports (the "next" import uses double quotes — keep that).

    The semicolons after the object and after export default must be preserved.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('next.config.ts','utf8'); console.log({hasExternal:c.includes(\"serverExternalPackages\"),hasTracing:c.includes(\"outputFileTracingIncludes\"),hasOldKey:c.includes(\"serverComponentsExternalPackages\")})"</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts contains the string "serverExternalPackages" (not "serverComponentsExternalPackages")
    - next.config.ts contains "outputFileTracingIncludes"
    - next.config.ts contains "'better-sqlite3'" as a value in the serverExternalPackages array
    - next.config.ts contains "'./data/cities.db'" as a value in outputFileTracingIncludes
    - next.config.ts does NOT contain "serverComponentsExternalPackages"
    - The file still has "import type { NextConfig } from" on the first line
    - The file still has "export default nextConfig" on the last line
    - npx tsc --noEmit exits 0 (TypeScript accepts the config shape)
  </acceptance_criteria>
  <done>next.config.ts updated with both required keys and TypeScript compilation passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json → Railway build | better-sqlite3 must be in dependencies so Railway's npm install compiles the native binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | next.config.ts serverExternalPackages misconfiguration | mitigate | Use exact key name "serverExternalPackages" verified against bundled docs; add TypeScript compile check (tsc --noEmit) to catch invalid key shapes before deploy |
| T-02-02 | Tampering | data/cities.db exposed as static download | accept (mitigated by D-03) | DB is placed at data/cities.db (project root), not in public/ — Next.js does not serve files outside public/ as static assets |
| T-02-03 | Information Disclosure | better-sqlite3 in devDependencies skipped on Railway | mitigate | Enforce better-sqlite3 is in "dependencies" (runtime), not "devDependencies"; acceptance criteria checks this explicitly |
</threat_model>

<verification>
After both tasks complete:

1. package.json check:
   node -e "const p=require('./package.json'); console.log(p.dependencies['better-sqlite3'], p.devDependencies['@types/better-sqlite3'], p.scripts['build-db'])"
   Expected: "^12.4.1"  "latest"  "node scripts/build-cities-db.mjs"

2. next.config.ts key check:
   grep -n "serverExternalPackages\|outputFileTracingIncludes" next.config.ts
   Expected: both keys present, no "serverComponentsExternalPackages"

3. TypeScript shape check:
   npx tsc --noEmit
   Expected: exits 0

4. node_modules check:
   ls node_modules/better-sqlite3/build/Release/*.node 2>/dev/null | head -1
   Expected: a .node file exists (native binary compiled locally)
</verification>

<success_criteria>
- better-sqlite3 is in package.json "dependencies" at version "^12.4.1"
- @types/better-sqlite3 is in package.json "devDependencies" at version "latest"
- "build-db" script entry exists in package.json "scripts"
- next.config.ts contains serverExternalPackages: ['better-sqlite3']
- next.config.ts contains outputFileTracingIncludes: { '/*': ['./data/cities.db'] }
- npm install succeeds locally
- npx tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-db-foundation/02-01-SUMMARY.md`
</output>
