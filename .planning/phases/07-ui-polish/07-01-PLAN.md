---
phase: 07-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SearchBar.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Typing 2+ characters and waiting 200ms shows a 'Searching…' row in the dropdown before the fetch resolves"
    - "The 'Searching…' row is hidden once the fetch resolves (results or empty state renders instead)"
    - "Rapid typing does not flicker the dropdown closed — isLoading stays true across aborted fetches"
    - "Screen readers announce the search input with the name 'Search for a city'"
  artifacts:
    - path: "src/components/SearchBar.tsx"
      provides: "isLoading state, Searching… row, aria-label attribute"
      contains: "isLoading"
  key_links:
    - from: "setTimeout callback (debounce handler)"
      to: "isLoading state"
      via: "setIsLoading(true) called at top of callback, setIsLoading(false) in finally block"
      pattern: "setIsLoading\\(true\\)"
    - from: "dropdown render"
      to: "isLoading boolean"
      via: "three-way ternary: isLoading → Searching… / results.length===0 → No cities found / else → results"
      pattern: "isLoading.*Searching"
---

<objective>
Add a "Searching…" loading indicator to SearchBar's dropdown panel and add the aria-label attribute to the search input.

Purpose: Close UI-01 (loading feedback from 200ms debounce fire until fetch resolves) and UI-02 (screen reader name for the combobox input) — the two SearchBar gaps from the v1.1 UI audit.
Output: Modified `src/components/SearchBar.tsx` with isLoading state, three-way dropdown render, and aria-label on the input element.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-polish/07-CONTEXT.md
@.planning/phases/07-ui-polish/07-PATTERNS.md
</context>

<interfaces>
<!-- Current SearchBar.tsx structure — executor reads the file directly; key contract points listed here. -->

useState block (lines 37–41 in current file):
  const [query, setQuery] = useState('')
  const [results, setResults] = useState<CityEntry[]>([])
  const [showDropdown, setShowDropdown] = useState(false)
  const [duplicateError, setDuplicateError] = useState(false)
  const [highlightedIndex, setHighlightedIndex] = useState(-1)

Debounce handler (lines 81–98): setTimeout callback contains AbortController creation,
  fetch call, if (res.ok) block with setResults + setShowDropdown(true), and catch block
  with AbortError guard.

Dropdown render (lines 168–200): guarded by {showDropdown && ...}, currently two-way
  ternary: results.length === 0 → empty-state <li> / else → results.map(...)

Input element (lines 150–162): has role="combobox", aria-expanded, aria-controls,
  aria-autocomplete, aria-activedescendant, value, onChange, onKeyDown, placeholder, className.
  Missing: aria-label.

Empty-state <li> template (lines 175–180):
  role="option" aria-disabled="true" className="px-4 py-2 text-sm text-gray-500"
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add isLoading state and wire debounce + finally + abort-guard</name>
  <files>src/components/SearchBar.tsx</files>

  <read_first>
    src/components/SearchBar.tsx — read the full file to understand current useState block
    (lines 37–41), the debounce setTimeout callback (lines 81–98), and the catch block
    AbortError guard (line 95). These are the three surgical insertion points.
    .planning/phases/07-ui-polish/07-CONTEXT.md — decisions D-02, D-03, D-06 govern
    exactly where setIsLoading calls are placed and where they are prohibited.
    .planning/phases/07-ui-polish/07-PATTERNS.md — "Debounce callback" section shows the
    exact insertion points with surrounding code.
  </read_first>

  <behavior>
    - After the existing useState hooks block (after line 41), a new hook exists:
      const [isLoading, setIsLoading] = useState(false)
    - At the top of the setTimeout callback body (before `const controller = new AbortController()`),
      two calls appear in order: setIsLoading(true) then setShowDropdown(true)
    - A finally block wraps the try/catch, containing only: setIsLoading(false)
    - The AbortError guard branch (catch err where err.name === 'AbortError') has NO
      setIsLoading(false) call — it returns immediately, leaving isLoading true
    - TypeScript compiles clean: npx tsc --noEmit exits 0
  </behavior>

  <action>
    Read src/components/SearchBar.tsx in full first.

    1. Add isLoading state — insert `const [isLoading, setIsLoading] = useState(false)` on
       the line immediately after `const [highlightedIndex, setHighlightedIndex] = useState(-1)`
       (currently line 41). Match the existing indentation (2-space, no semicolons).

    2. Wire setIsLoading(true) at debounce fire — at the very top of the setTimeout async
       callback body (before `const controller = new AbortController()`), insert two lines:
         setIsLoading(true)
         setShowDropdown(true)
       These must be the first two statements inside the callback, per D-02 and D-05.
       Note: the existing `setShowDropdown(true)` inside the `if (res.ok)` block (currently
       line 92) stays in place — it is harmless and will be shadowed by the isLoading check
       in Task 2.

    3. Add finally block — wrap the existing try/catch in a try/catch/finally. The finally
       block contains only `setIsLoading(false)`. The catch block must remain unchanged —
       specifically the AbortError guard `if (err instanceof Error && err.name === 'AbortError') return`
       must NOT have setIsLoading(false) before its return statement (per D-03 and D-06).
       The finally block runs after the AbortError return, which is the correct behavior for
       resetting loading on the final successful fetch.

    Verify TypeScript is clean after this task before proceeding.
  </action>

  <verify>
    <automated>cd "/home/aiadmin/Documents/frontire AI/Frontire-AI-Coding-Group3" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - src/components/SearchBar.tsx contains the line: const [isLoading, setIsLoading] = useState(false)
    - src/components/SearchBar.tsx contains setIsLoading(true) inside the setTimeout callback
      body, before the AbortController creation line
    - src/components/SearchBar.tsx contains setIsLoading(false) inside a finally block
    - src/components/SearchBar.tsx does NOT contain setIsLoading(false) between the
      AbortError guard and its return statement
    - npx tsc --noEmit exits 0 with no errors
  </acceptance_criteria>

  <done>isLoading state declared, set true at debounce fire, reset in finally, not reset on abort. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire isLoading into dropdown render + add aria-label to input</name>
  <files>src/components/SearchBar.tsx</files>

  <read_first>
    src/components/SearchBar.tsx — read after Task 1 edits are in place. Focus on the
    dropdown render block (currently lines 168–200) and the input element (lines 150–162).
    These are the two remaining insertion points.
    .planning/phases/07-ui-polish/07-CONTEXT.md — decisions D-01 (Searching… row structure),
    D-04 (three-way priority: isLoading / empty / results), D-07 (aria-label value).
    .planning/phases/07-ui-polish/07-PATTERNS.md — "Dropdown render priority" section shows
    the exact three-way ternary structure. "aria-label insertion point" section shows prop
    placement after aria-autocomplete="list".
  </read_first>

  <action>
    Read the current state of src/components/SearchBar.tsx after Task 1 edits.

    1. Replace the two-way dropdown ternary with a three-way ternary implementing priority
       from D-04:
         - Priority 1 (isLoading is true): render the Searching… <li> row
         - Priority 2 (results.length === 0): render the existing "No cities found" <li> row
         - Priority 3 (else): render results.map(...)
       The Searching… row uses the IDENTICAL structure as the empty-state row per D-01:
         <li role="option" aria-disabled="true" className="px-4 py-2 text-sm text-gray-500">
           Searching…
         </li>
       The ellipsis character is the Unicode horizontal ellipsis U+2026 (…), not three dots.
       Do NOT use aria-selected or any additional attributes — match the empty-state row exactly.
       The outer <ul> element and its className remain unchanged.

    2. Add aria-label to the input element — insert `aria-label="Search for a city"` as a
       new JSX prop on the <input> element. Place it after `aria-autocomplete="list"` and
       before `aria-activedescendant`, maintaining the grouping convention of ARIA attributes
       before event handlers (per D-07 and the PATTERNS.md insertion point).

    Run TypeScript check and a grep confirmation after edits.
  </action>

  <verify>
    <automated>cd "/home/aiadmin/Documents/frontire AI/Frontire-AI-Coding-Group3" && npx tsc --noEmit 2>&1 | head -20 && grep -n "isLoading" src/components/SearchBar.tsx && grep -n 'aria-label="Search for a city"' src/components/SearchBar.tsx && grep -n "Searching" src/components/SearchBar.tsx</automated>
  </verify>

  <acceptance_criteria>
    - src/components/SearchBar.tsx contains the JSX expression: {isLoading ? (
      as the first branch inside the dropdown <ul> render, before results.length === 0 check
    - src/components/SearchBar.tsx contains: Searching… (Unicode ellipsis U+2026) inside
      a <li role="option" aria-disabled="true" className="px-4 py-2 text-sm text-gray-500">
    - src/components/SearchBar.tsx contains: aria-label="Search for a city" on the <input>
      element, placed after aria-autocomplete="list"
    - npx tsc --noEmit exits 0 with no errors
    - grep -c "font-semibold" src/components/SearchBar.tsx returns 0 (no regressions)
  </acceptance_criteria>

  <done>
    Dropdown renders Searching… when isLoading is true, No cities found when results empty,
    results list otherwise. Input has aria-label="Search for a city". TypeScript clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → SearchBar state | query string entered by user drives fetch and UI state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Denial of Service | debounce/AbortController | accept | 200ms debounce + AbortController already in place from Phase 04; no new attack surface introduced |
| T-07-02 | Information Disclosure | "Searching…" dropdown row | accept | Row reveals only that a search is in-flight; no city data or credentials exposed |
| T-07-03 | Tampering | aria-label attribute | accept | Static string attribute; no user-controlled content; no injection risk |
</threat_model>

<verification>
After both tasks complete:

1. TypeScript check: `npx tsc --noEmit` exits 0
2. Source assertions:
   - `grep -n "isLoading" src/components/SearchBar.tsx` shows state declaration, setIsLoading(true) in debounce, setIsLoading(false) in finally
   - `grep -n "Searching" src/components/SearchBar.tsx` shows the Searching… <li> row
   - `grep -n 'aria-label="Search for a city"' src/components/SearchBar.tsx` returns 1 match on the input element
   - `grep -c "setIsLoading(false)" src/components/SearchBar.tsx` returns 1 (only the finally block — not in the AbortError branch)
3. Manual smoke test (optional): Type 2+ characters in SearchBar, wait 200ms — dropdown opens with "Searching…" before results arrive
</verification>

<success_criteria>
- UI-01: isLoading boolean added; dropdown shows "Searching…" from 200ms debounce fire until fetch resolves/rejects; isLoading stays true across rapid-typing aborts (no flicker)
- UI-02: input element carries aria-label="Search for a city"
- TypeScript compiles clean; no regressions in existing SearchBar behavior
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-polish/07-01-SUMMARY.md` using the summary template.
</output>
