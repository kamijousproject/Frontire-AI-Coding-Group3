---
phase: 07-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/WeatherCard.tsx
autonomous: true
requirements:
  - UI-03

must_haves:
  truths:
    - "The city name on every weather card renders at font-weight 700 (font-bold), matching the temperature line"
    - "No font-semibold class remains anywhere in WeatherCard.tsx"
  artifacts:
    - path: "src/components/WeatherCard.tsx"
      provides: "font-bold city name — 2-weight typography consistent with spec (400 regular, 700 bold)"
      contains: "font-bold"
  key_links:
    - from: "WeatherCard city name <p> element"
      to: "Tailwind font-bold utility"
      via: "className string on line 37"
      pattern: "text-lg font-bold leading-tight"
---

<objective>
Change the city name `<p>` element in WeatherCard from `font-semibold` to `font-bold`.

Purpose: Close UI-03 — the typography spec requires exactly two font weights (400 regular, 700 bold). The city name currently uses font-semibold (600), which deviates from the spec and is the only non-conforming occurrence in the file.
Output: Modified `src/components/WeatherCard.tsx` with font-bold on line 37 (city name), matching the temperature line (line 43) which already uses font-bold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-polish/07-CONTEXT.md
@.planning/phases/07-ui-polish/07-PATTERNS.md
</context>

<interfaces>
<!-- WeatherCard.tsx — relevant section for this plan. -->

Line 37 (target — only font-semibold in the file):
  <p className="text-lg font-semibold leading-tight">{data.city}</p>

Line 43 (reference — font-bold already established for emphasis):
  <p className="text-2xl font-bold">{data.temp_c}°C / {data.temp_f}°F</p>

After the change, line 37 becomes:
  <p className="text-lg font-bold leading-tight">{data.city}</p>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace font-semibold with font-bold on the city name element</name>
  <files>src/components/WeatherCard.tsx</files>

  <read_first>
    src/components/WeatherCard.tsx — read the full file (73 lines) to confirm the exact
    className string on line 37 and verify this is the only font-semibold occurrence.
    .planning/phases/07-ui-polish/07-CONTEXT.md — decision D-08: "Change font-semibold →
    font-bold on the city name <p> element (currently line 37). This is the only
    font-semibold occurrence in the file."
    .planning/phases/07-ui-polish/07-PATTERNS.md — "Target line" section shows before/after.
  </read_first>

  <action>
    Read src/components/WeatherCard.tsx in full.

    Locate the <p> element rendering {data.city} — it has className="text-lg font-semibold leading-tight".
    Change the className token `font-semibold` to `font-bold`. The full resulting className
    string is: "text-lg font-bold leading-tight"

    No other lines in the file change. No other className, import, prop, or JSX structure
    is touched. This is a single token substitution on a single line.

    After the edit, run a grep to confirm zero remaining font-semibold occurrences and
    a TypeScript check to confirm no regressions.
  </action>

  <verify>
    <automated>cd "/home/aiadmin/Documents/frontire AI/Frontire-AI-Coding-Group3" && grep -c "font-semibold" src/components/WeatherCard.tsx && grep -n "font-bold" src/components/WeatherCard.tsx && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "font-semibold" src/components/WeatherCard.tsx returns 0
    - grep -n "font-bold" src/components/WeatherCard.tsx shows at least 2 lines: the city
      name <p> (formerly font-semibold) and the temperature <p> (line 43, unchanged)
    - The city name line reads exactly: className="text-lg font-bold leading-tight"
    - npx tsc --noEmit exits 0 with no errors
    - No other lines in WeatherCard.tsx differ from the pre-edit state
  </acceptance_criteria>

  <done>
    font-semibold replaced with font-bold on the city name element. Zero font-semibold
    occurrences remain in WeatherCard.tsx. TypeScript clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WeatherData.city string → DOM render | city name from API response rendered as text content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | WeatherCard className | accept | Pure presentational change (CSS class token); no logic, no data flow, no new attack surface |
</threat_model>

<verification>
After task completes:

1. `grep -c "font-semibold" src/components/WeatherCard.tsx` → 0
2. `grep -n "font-bold" src/components/WeatherCard.tsx` → shows city name line AND temperature line
3. `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- UI-03: WeatherCard.tsx uses font-bold on the city name; no font-semibold class remains in the file
- Typography is consistent: both city name (font-bold 700) and temperature (font-bold 700) use the bold tier; all other text uses the regular tier
- TypeScript compiles clean; no regressions in WeatherCard behavior
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-polish/07-02-SUMMARY.md` using the summary template.
</output>
